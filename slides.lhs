Boolean expressions

Primitives:

  * is a number greater than another?
  * is mario touching this sprite?

Combinators:

  * x && y
  * x || y
  * !x
  * any(predicate, list)
  * all(predicate, list)


















































