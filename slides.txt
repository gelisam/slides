*------------------------*
|   git merge --rebase   |
*------------------------*

Before the merge:

  rm -rf ../sourcetree-repo && cp -r . ../sourcetree-repo


Linearize the history:

  git pull --rebase
  
  git push


The result:

  rm -rf ../sourcetree-repo && cp -r . ../sourcetree-repo

