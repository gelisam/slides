*------------------------*
|   git commit --amend   |
*------------------------*

This time, instead of fixing a bug, let's refactor:

  cat main.cpp
#include "hello.h"

int main() {
  say_hello("world");
  return 0;
}
 
  cat hello.h
#ifndef HELLO_H
#define HELLO_H

void say_hello(char* to_whom);

#endif
  
  cat hello.cpp
  
