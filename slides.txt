*------------------------*
|   git commit --amend   |
*------------------------*

This time, instead of fixing a bug, let's refactor:

  cat main.cpp
#include "hello.h"

int main() {
  say_hello("world");
  return 0;
}
 
  cat hello.h
  
  cat hello.cpp
  
