*------------------------*
|   git merge --rebase   |
*------------------------*

Before the merge:

  git log --graph --oneline --all --decorate


Linearize the history:

  git pull --rebase
  
  git push


The result:

  git log --graph --oneline --all --decorate

