*------------------------*
|   git commit --amend   |
*------------------------*

Rewrite the commit instead:

  git add hello.*
(no output)
  
  git commit --amend -am "refactor to hello.{h,c}"
  
  git log --stat
  
