*------------------------*
|   git commit --amend   |
*------------------------*

This time, instead of fixing a bug, let's refactor:

  cat main.cpp
#include <stdio.h>

void say_hello(char* to_whom) {
  printf("hello %s\n", to_whom);
}

int main() {
  say_hello("world");
  return 0;
}
 
  cat hello.h
  
  cat hello.cpp
  
