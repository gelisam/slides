import Control.Category

class Category k => Arrow k where
  arr   :: (a -> b) -> k a b
  first :: k a b    -> k (a,r) (b,r)































































main :: IO ()
main = putStrLn "done."
