div' :: Int -> Int -> Maybe Int
div' _ 0 = Nothing
div' x y = Just (x `div` y)

doMath :: Int -> IO ()
doMath n = do
    case n `div'` 2 of
      Nothing -> error "never happens"
      Just x  -> print (x + 1)

main :: IO ()
main = doMath 42


































































































