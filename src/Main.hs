import Control.Concurrent.MVar
import Control.Monad.Trans.State
import Data.IORef
import Text.Printf


data Mutex a = Mutex (MVar a)

newMutex :: a -> IO (Mutex a)

withMutex :: Mutex s -> State s a -> IO a

































































































main :: IO ()
main = return ()
